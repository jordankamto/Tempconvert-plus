import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  resultText: {
    fontSize: 79,
    color: "white",
    fontWeight: "bold",
    textAlign: "center",
  },
});

export { styles };
